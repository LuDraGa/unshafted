# Unshafted v0.6.7 to v0.8.0 Roadmap Handoff

**Status:** Active
**Last updated:** 2026-04-27
**Baseline branch:** `consolidation`
**Current baseline commit:** `4a999d7 feat(ux): simplify result attention hierarchy`

This document is the durable execution reference for the stabilization and product-clarity work that follows v0.6.7. Use it after context compaction, handoff, or branch switching to understand what has shipped, what remains, and what should happen next.

## Product Direction

Unshafted should become a trustworthy, decision-first contract risk triage tool:

- First-time users should understand the value before technical setup.
- Users should know exactly where contract text, API keys, auth tokens, Drive files, and analysis results go.
- Results should answer the practical signing question first, then expose detail.
- The popup should remain the quick-action surface, not an overloaded full report surface.
- BYOK remains the real scan mechanism through v0.7.0.
- Drive backup is explicit opt-in, not an automatic side effect of sign-in.
- v1 remains upload-first unless current-page analysis is deliberately restored.

## Completed Baseline

These commits form the current security/stabilization and v0.7 product-clarity baseline:

| Commit | Area | Summary |
|---|---|---|
| `0955a74` | Security | Stops bundling provider secrets by replacing full env injection with an allowlisted public env and removing default provider keys. |
| `15acae6` | Security | Hardens OAuth state validation, token persistence order, Drive backup opt-in, sensitive source-file handling, Supabase profile policy, and storage behavior. |
| `5ba2f31` | Reliability | Stabilizes quick/deep analysis state, provider retry behavior, background message errors, storage races, and coverage metadata. |
| `721e699` | UX | Adds no-key demo/sample path, visible history basics, sync/privacy controls, decision-first result summary, and clearer first-run setup. |
| `3038b4f` | CI | Makes lint verification pass, excludes minified worker assets from ESLint, and aligns formatting/import/export rules. |
| `3c5a9ec` | Docs | Adds this durable v0.8 roadmap handoff. |
| `8d29be6` | History | Improves the report workflow with richer recent analyses, copy/export, delete, and sync status affordances. |
| `897f6b1` | History | Saves quick scans into recent reports immediately. |
| `8d99e8c` | History | Clarifies recent report actions and opened-report focus behavior. |
| `cca7769` | Sync | Prompts before backing up the current report when Drive is enabled. |
| `700d077` | Sync | Replaces hidden native prompt behavior with an in-popup current-report backup prompt. |
| `e40423c` | History | Replaces native delete confirmation with inline delete confirmation. |
| `01195b2` | History | Renders opened quick-scan-only reports instead of blank metadata-only views. |
| `dc17b64` | History | Aligns opened report layout with the live quick-scan result structure. |
| `d2ad923` | Docs | Updates v0.7 roadmap status after history/report iteration. |
| `449373c` | Results | Adds decision-first action summaries for quick and detailed analysis. |
| `7b79ea0` | Sync | Confirms Drive backup state after successful Drive writes and fixes OpenAI deep-analysis schema references. |
| `4a999d7` | UX | Simplifies result attention hierarchy by making the decision card the anchor and removing duplicate summaries. |

Verification at baseline:

- `pnpm lint` passes with Node `22.15.1`.
- `pnpm type-check` passes with Node `22.15.1`.
- `pnpm build` passes with Node `22.15.1`.
- `pnpm --filter @extension/unshafted-core test` passes.
- `git diff --check` passes.
- Built `dist` contains no provider env key names and no real key-like `sk-proj-*` or `sk-or-v1-*` values.

## Release Path

### v0.6.8: Stabilization Release

Goal: ship the trust and correctness fixes without expanding product scope.

Include:

- The five baseline commits listed above.
- Any small release-version/package metadata updates required for v0.6.8.
- Release notes focused on security, privacy, stale-result correctness, and verification.

Do not include:

- Billing, hosted credits, full web app, collaboration, new contract templates, or a large report redesign.
- Current-page analysis work unless upload-first positioning is explicitly reversed.

Before release:

- Provider keys that may have existed in `.env` while public builds were produced from vulnerable commits have been rotated externally.
- Confirm Chrome Web Store package is built from the fixed commit.
- Re-run bundle secret scan against the final release artifact.

### v0.7.0: Product Clarity and Reuse

Goal: turn the capable extension into a product normal users can understand, trust, and reuse.

Primary initiatives:

- Strengthen the first-run demo and setup path so users see value before BYOK setup.
- Make the result experience decisively answer: likely okay, negotiate first, pause, or get legal help.
- Mature the history/report loop with reopen, delete, copy/export, and local/Drive status.
- Separate sign-in from Drive backup everywhere in product language and controls.
- Simplify deep-analysis taxonomy into user-task groups.

Do not include:

- Complex Drive document manager.
- Hosted LLM billing or credits.
- Enterprise/team workflows.
- Broad legal workflow platform features.

### v0.8.0: System and Design Maturity

Goal: reduce product/design debt after the v0.7 core loop is settled.

Primary initiatives:

- Consolidate semantic design tokens and shared component variants.
- Mature report/export presentation.
- Resolve upload-only vs current-page analysis positioning and remove dead promises.
- Improve local dev/CI onboarding beyond `.nvmrc` if needed.
- Add migration workflow clarity if Supabase schema continues expanding.

Do not include:

- Lawyer handoff network.
- Negotiation automation marketplace.
- Multi-user workspaces.
- Enterprise admin features.

## Priority Backlog

### P0: Stabilization and Trust

Status: implemented in baseline.

Closed problems:

- Full `.env` injection into extension bundles.
- Provider API-key defaults in client code.
- OAuth token persistence before Supabase auth success.
- Missing OAuth state validation.
- Original source file base64 persistence by default.
- Stale deep-analysis display after re-scan.
- Over-broad structured LLM retry behavior.
- Background message handlers leaving callers unresolved.
- Hidden Drive backup mental model.
- Blocked lint/build/type-check verification.

Remaining P0 release work:

- Prepare v0.6.8 release metadata and notes.
- Confirm final release artifact with bundle scans.

### P1: Product Clarity and Reuse

Status: partially implemented.

Initiatives to execute next:

1. **History and Report Workflow**
   Goal: make saved analyses feel durable and controllable.
   Scope: medium to large.
   Status: implemented; manual QA passed for local quick scans, Drive-backed scans, open, delete confirmation, copy/export, and opened report rendering.
   Approach completed: recent analyses show quick/deep reports, copy/export/delete are available, delete and current Drive backup use inline confirmations, local/Drive status is visible with requested/backed-up/restored states, Drive refresh is user-triggered, and opened reports align with the live quick-scan layout.

2. **Decision-First Result Refinement**
   Goal: make users understand the next action before reading details.
   Scope: medium.
   Status: implementation pass 1 complete; manual QA passed for quick-scan and opened-report consistency.
   Approach completed: live and saved quick scans share a decision-first summary with recommendation, rationale, coverage, top blockers, and asks; detailed results reuse the same action-summary structure.

3. **Guided Setup and Demo Polish**
   Goal: make first value happen before technical setup anxiety.
   Scope: medium.
   Status: implementation pass 1 complete.
   Approach completed: sample-result entry exists without API setup, no-key state uses setup as the primary CTA, and popup/options setup copy now explains progress, provider-key rationale, local key storage, provider calls, and the return-to-popup path.

4. **Privacy and Sync Copy Pass**
   Goal: prevent trust collapse from hidden data movement.
   Scope: small to medium.
   Status: implementation pass 1 complete.
   Approach completed: popup/options/README/privacy policy now align around local API keys, provider-bound contract text, sign-in-gated deep analysis, explicit Drive backup, and non-backfilled old source files.

5. **Attention Simplification**
   Goal: reduce simultaneous attention targets in the popup while preserving utility.
   Scope: medium.
   Status: implementation pass 1 complete; manual QA passed for duplicate-summary removal and opened report alignment. Larger calming redesign deferred to v0.8.
   Approach completed: decision card is the result anchor, duplicate summary accordions were removed, and document metadata moves below the result after quick scan exists.

### P2: System and Scope Cleanup

Status: not started beyond lint baseline.

Initiatives:

1. **Design System Consolidation**
   Goal: prevent UI drift.
   Scope: medium.
   Approach: define semantic tokens for brand, risk, selection, status, focus, and standardize button/input/chip/badge/alert/accordion variants.

2. **Scope Cleanup**
   Goal: make the product easier to explain and maintain.
   Scope: medium.
   Approach: decide whether page analysis is a supported v1 feature. If not, remove or hide user-facing promises and keep code paths clearly secondary.

3. **Dev/CI Onboarding**
   Goal: keep verification repeatable.
   Scope: small.
   Approach: `.nvmrc` is tracked. Consider adding Volta/mise docs or a bootstrap script if contributors keep landing on Node 18.

## Execution Docs

Use one short execution doc per product workstream. Current set:

1. `execution-docs/v0.7-history-report-workflow.md`
2. `execution-docs/v0.7-decision-first-results.md`
3. `execution-docs/v0.7-guided-setup-demo.md`
4. `execution-docs/v0.7-privacy-sync-copy.md`
5. `execution-docs/v0.7-attention-simplification.md`
6. `execution-docs/v0.8-design-system.md`
7. `execution-docs/v0.8-scope-cleanup.md`

Future execution docs should include:

- Goal and non-goals.
- User-facing behavior.
- Files likely to change.
- Data/storage impacts.
- UX copy decisions.
- Verification checklist.
- Handoff notes and open questions.

## Coordination Notes

Product and design coordination required:

- Decision banner language and severity posture.
- Report/share/export tone: keep the brand memorable, but make shared reports neutral and credible.
- History UX boundaries: popup should expose recent reports and actions, not become a full archive manager.

Engineering coordination required:

- Result schema changes must preserve backward compatibility with local history and Drive records.
- Drive deletion/export/history changes need one consistent local-vs-backed-up mental model.
- Any page-analysis cleanup must avoid breaking existing content-script build paths unless the feature is intentionally removed.

## Key Files

High-touch product surfaces:

- `pages/popup/src/Popup.tsx`
- `pages/popup/src/components/AnalysisWorkspace.tsx`
- `pages/popup/src/components/ResultCards.tsx`
- `pages/options/src/Options.tsx`
- `pages/content/src/matches/all/index.ts`

Core data and analysis:

- `packages/unshafted-core/lib/schemas.ts`
- `packages/unshafted-core/lib/types.ts`
- `packages/unshafted-core/lib/document.ts`
- `packages/unshafted-core/lib/prompts.ts`
- `packages/unshafted-core/lib/runtime.ts`
- `packages/shared/lib/utils/analysis-workflow.ts`

Persistence and sync:

- `packages/storage/lib/impl/unshafted-analysis-storage.ts`
- `packages/storage/lib/impl/unshafted-history-storage.ts`
- `packages/storage/lib/impl/unshafted-settings-storage.ts`
- `packages/supabase/lib/drive.ts`
- `packages/supabase/lib/drive-sync.ts`
- `packages/supabase/lib/drive-token.ts`
- `packages/supabase/lib/auth.ts`

Trust and release:

- `README.md`
- `privacy-policy.md`
- `chrome-extension/vite.config.mts`
- `packages/vite-config/lib/with-page-config.ts`
- `eslint.config.ts`
- `.nvmrc`

## Standard Verification

Run these before committing meaningful changes:

```bash
source ~/.nvm/nvm.sh
nvm use 22.15.1
pnpm lint
pnpm type-check
pnpm build
pnpm --filter @extension/unshafted-core test
git diff --check
rg -n "CEB_OPENROUTER_API_KEY|CEB_OPENAI_API_KEY|OPENROUTER_API_KEY|OPENAI_API_KEY" dist -g '!**/*.map'
rg -n "sk-proj-[A-Za-z0-9_-]{20,}|sk-or-v1-[A-Za-z0-9_-]{20,}" dist -g '!**/*.map'
```

Expected result for the two `rg` commands is exit code `1` with no matches.

## Open Questions

- Should v0.6.8 be tagged before any v0.7 product work begins?
- Should v0.8 move longer reports to a dedicated extension page instead of keeping them entirely in-popup?
- Should current-page analysis be kept as a quiet power feature or removed from positioning until it is first-class?
- What contract category should v0.7 optimize for in copy and examples: freelance/SOW, rental, employment, NDA, or general-purpose?
